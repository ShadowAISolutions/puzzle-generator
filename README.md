# puzzle-generator

Placeholder. This repository will hold a machine-generated corpus of logic puzzles, every
one of them validated by an independent solver for solvability, solution uniqueness and
difficulty band.

This file exists so the repository has a `main` branch, which GitHub Pages needs before it
can be pointed at one. The first session replaces it.
